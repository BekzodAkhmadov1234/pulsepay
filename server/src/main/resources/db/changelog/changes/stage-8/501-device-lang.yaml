databaseChangeLog:
  - changeSet:
      id: 501-device-lang
      author: pulsepay
      comment: >
        Add lang column to devices table to store the user's preferred UI language
        per device. Supported values: uz, ru, en, zh, uz_c.
        Defaults to 'uz' (most common for Uzbek market).
      changes:
        - addColumn:
            tableName: devices
            columns:
              - column:
                  name: lang
                  type: VARCHAR(5)
                  defaultValue: 'uz'
                  constraints:
                    nullable: false
        - sql:
            sql: >
              ALTER TABLE devices
              ADD CONSTRAINT chk_devices_lang
              CHECK (lang IN ('uz', 'ru', 'en', 'zh', 'uz_c'));
